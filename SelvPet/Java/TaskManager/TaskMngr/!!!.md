Непонятные штуки с задания которые стоит подучить!!

    1.   Optional<Task> opt = tasks.stream().filter(t -> t.getId() == id).findFirst(),
    2. input.equalsIgnoreCase
    3. input.substring
    4. try {
                    int id = Integer.parseInt(input.substring(5).trim());
                    manager.markDone(id);
                } catch (NumberFormatException e) {
                    System.out.println("Incorrect format of ID.");
                }
    5. 
